import { FullDatePipePipe } from './full-date-pipe.pipe';

describe('FullDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FullDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
