import { RandomBackgroundStyleDirective } from './random-background-style.directive';

describe('RandomBackgroundStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomBackgroundStyleDirective();
    expect(directive).toBeTruthy();
  });
});
