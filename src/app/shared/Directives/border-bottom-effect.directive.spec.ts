import { BorderBottomEffectDirective } from './border-bottom-effect.directive';

describe('BorderBottomEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderBottomEffectDirective();
    expect(directive).toBeTruthy();
  });
});
